{
  "name": "plantcare-back",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backend for the Plant Care Agent. Calls Gemini (multimodal) and returns a structured PlantCareResult.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.1",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
